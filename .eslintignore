# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
public
scripts
# don't lint tailwind config
tailwind.config.js
# don't lint the secret cow level
src/views/SecretCowLevel.svelte
